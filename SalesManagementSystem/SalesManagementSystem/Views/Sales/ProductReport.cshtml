@model IEnumerable<SalesManagementSystem.Models.Product>

@{
    ViewBag.Title = "ProductReport";
    Layout = "~/Views/Shared/_dashboard.cshtml";
}
<hr />
<hr />
<hr />
<hr />
<hr />
<h2 class="text-center text-primary">Product Report</h2>
<div class="container">
    <div class="row">
        <div class="col-lg-12">
            <table class="table table-striped table-hover table-bordered">
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.pname)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.pcompany)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.ptype)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.price)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.stock)
                    </th>
                    <th></th>
                </tr>

                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.pname)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.pcompany)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.ptype)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.price)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.stock)
                        </td>
@*                        <td>
                            @Html.ActionLink("Edit", "Edit", new { id = item.id }) |
                            @Html.ActionLink("Details", "Details", new { id = item.id }) |
                            @Html.ActionLink("Delete", "Delete", new { id = item.id })
                        </td>*@
                    </tr>
                }

            </table>
</div>
</div></div>